docker logs -f 778a92a22b96
sudo docker start 778a92a22b96
EJECUTAR Y CREAR CONTENEDOR:
docker run <nameImagen>
CREAR IMAGEN APARTIR DE UN DOCKER FILE
docker build -t <nameImagen> .

DOCKER REDES
docker run --network host -d <nameImages>

EL CONTENEDOR VA A TENER LA IP PRIVADA DE MI HOST 